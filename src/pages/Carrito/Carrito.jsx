import styles from './Carrito.module.css';

function Carrito() {
    return (
        <div className={styles.contenedorCarrito}>
            <h1 className={styles.mensaje}>⚠️ Esto no entra en la Pre-Entrega ⚠️</h1>
        </div>
    );
}

export default Carrito;